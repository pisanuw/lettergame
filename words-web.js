// Words added via the admin web interface.
// Format: array of { lang, category, word } objects.
// These are merged into the game at runtime and periodically
// folded into the main word files via merge-web-words.js.
const WEB_WORDS = [];
